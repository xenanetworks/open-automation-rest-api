title: Common Options
type: object
properties:
    duration_type:
        $ref: "./DurationType.yaml"
    duration:
        description: The test duration value
        type: number
    duration_unit:
        $ref: "./DurationUnit.yaml"
    repetitions:
        description: The number of times each test with the same parameters to repeat
        minimum: 1
        type: integer
required:
    - repetitions
    - duration_type
    - duration_unit
    - duration
