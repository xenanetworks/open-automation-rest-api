title: ValueRange
type: object
properties:
  start_value:
    title: Start Value
    type: integer
  step_value:
    title: Step Value
    type: integer
  stop_value:
    title: Stop Value
    type: integer
  action:
    $ref: './ModifierActionOption.yaml'
  restart_for_each_port:
    title: Restart For Each Port
    type: boolean
required:
  - start_value
  - step_value
  - stop_value
  - action
  - restart_for_each_port